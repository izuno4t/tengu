name: core coverage gate

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  core-coverage:
    name: enforce per-file core C2 coverage threshold
    runs-on: ubuntu-latest
    env:
      COVERAGE_MIN_BRANCHES: "90"
      COVERAGE_TOOLCHAIN: "nightly"
      COVERAGE_IGNORE_REGEX: "src/(cli\\.rs|tui/.*|llm/(anthropic|google|openai|ollama)\\.rs|mcp/(http|stdio)\\.rs)"
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: llvm-tools-preview

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Use rustup LLVM tools
        shell: bash
        run: |
          set -euo pipefail
          host="$(rustc -vV | sed -n 's/^host: //p')"
          tools_dir="$(rustc --print sysroot)/lib/rustlib/${host}/bin"
          echo "LLVM_COV=${tools_dir}/llvm-cov" >> "$GITHUB_ENV"
          echo "LLVM_PROFDATA=${tools_dir}/llvm-profdata" >> "$GITHUB_ENV"
          echo "${tools_dir}" >> "$GITHUB_PATH"
          "${tools_dir}/llvm-cov" --version
          "${tools_dir}/llvm-profdata" --version

      - name: Check per-file core C2 coverage
        run: scripts/coverage.sh branch-lcov
